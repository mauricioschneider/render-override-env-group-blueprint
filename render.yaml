services:
  - type: web
    name: preview-sync
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: TEST_SYNC
        sync: false
      - fromGroup: previewVars
      
previews:
  generation: automatic